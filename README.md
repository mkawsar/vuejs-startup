## Build Setup

### install dependencies
`npm install`
### development
`npm run dev`
### production
`npm run build`
### run unit tests
`npm run unit`
### run and watch unit tests
`npm run unit:watch`
